import React from "react"
import LoginClient from "./login-client"

export const metadata = {
    title: "Admin Login | EnKash Glossary",
    description: "Secure login for EnKash admin portal",
}

const AdminPage = () => {
    return <LoginClient />
}

export default AdminPage